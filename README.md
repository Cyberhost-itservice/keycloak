# Keycloak 20.0.0 and Mariadb 10.3 on Ubuntu Server 20.4 
keycloak+mariadb \
For install : ansible-pull h-U ttps://github.com/Cyberhost-itservice/keycloak.git \
For install without packages - make install.sh and copy the input
This is a Install from Keycloak about ansible. \
System: Ubuntu 20.04 Server and Ubuntu live System \
Set IP in templates/keycloak.conf.j2 of our ip . \
\
openssl req -newkey rsa:4096 -new -nodes -x509 -days 3650 -keyout key.pem -out cert.pem \
this is not finish \
\
when question : info@cyberhost-itservice.com \
